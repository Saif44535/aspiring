import AdminPage from "@/components/Admin/AdminPage";

export const metadata = {
  title: "Aspiring Future Consultancy",
  description:
    "Register yourself with us to find the best universities all across the globe.",
};

const Admin = () => {
  return <AdminPage />;
};

export default Admin;
